import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duoc.sicma',
  appName: 'SICMA',
  webDir: 'dist/sicma-frontend/browser',
};

export default config;
